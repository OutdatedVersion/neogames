# Prevent parent folder(s) traversal looking for a configuration file
config.stopBubbling = true


# Use chaining by default
lombok.accessors.chain = true

# Ignore (get/set) and instead use a fluent approach
lombok.accessors.fluent = true